class snowFlakes {
  constructor(canvas, y) {
    this.size = 30;
    this.canvas = canvas;
    this.ctx = this.canvas.getContext("2d");
    this.x = this.canvas.height; //maybe I don't need as they are falling from Y 
    this.y = y;
    this.speed = 5;
    this.direction = 1;
    img.src = "https://i.pinimg.com/474x/0b/1a/8f/0b1a8f342eb777231fa8f4331010abcc.jpg";
  }

class gifts {
    constructor(canvas, y) {
      this.size = 30;
      this.canvas = canvas;
      this.ctx = this.canvas.getContext("2d");
      this.x = this.canvas.height; // maybe I don't need as they are falling from Y 
      this.y = y;
      this.speed = 5;
      this.direction = 1;
      img.src = "https://www.teteamodeler.com/assets/coloriages/pixel-art-cadeau-de-nol.jpeg";
    }

